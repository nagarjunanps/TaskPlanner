{
  "name": "gtrmy-taskplanner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names CLIENT,SERVER --prefix-colors cyan,yellow \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "install:all": "npm install && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
